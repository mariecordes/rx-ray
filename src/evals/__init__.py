"""Evaluation harness for the rx-ray pipeline (roadmap D3a/D3b/D4).

Consumes the structured QueryAnswerResponse the pipeline already returns;
production modules must never import from this package.
"""
